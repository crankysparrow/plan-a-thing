extends layout

block content
  h1= title
  p Welcome to #{title}

  p 
    a(href='/scheduler/event/create') Schedule an Event! 
  
  if events
    ul
      for event in events
        li 
          a(href= event.url)= event.title
